import 'package:flutter/material.dart';

class Settingscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      width: double.infinity,
      padding: EdgeInsets.all(20),
      
      
      child: const Center(
        
        child: Column(
          
          
          children: [
            Text("this is seeting page "),
          ],
        ),
      ),
    );
  }
}
